names = ["Jenny", "Alexus", "Sam", "Grace"]

heights = [61, 70, 67, 65]

names_and_heights = zip(names, heights)

# convert to a list:

converted_list = list(names_and_heights)


print(name_and_heights)


# The zip() function takes two (or more)
# lists as inputs and returns an object
# that contains a list of pairs. Each pair
#  contains one element from each of the
#  inputs.
